---
import Layout from "@/layout/Layout.astro";
import Swappp from "@/views/interiors/Swappp.astro";
import { getLangFromUrl } from "@/i18n/utils";

const lang = getLangFromUrl(Astro.url);
---

<Layout lang={lang} className="bg-[#f6f6f6]">
  <Swappp />
</Layout>
