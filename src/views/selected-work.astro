---
import { getLangFromUrl, useTranslations } from "@/i18n/utils";

const lang = getLangFromUrl(Astro.url);
const ui = useTranslations(lang);
---

<section class="">
  <h2 class="text-2xl text-center mx-auto w-45 font-normal">{ui.selected_work.title}</h2>
  
</section>