---
import ProjectCard from "@/components/projects/project-card.astro";
import { getLangFromUrl, useTranslations } from "@/i18n/utils";

const lang = getLangFromUrl(Astro.url);
const ui = useTranslations(lang);
---

<section class="mb-18 mt-21 lg:container lg:mx-auto lg:px-4 pb-7 lg:pb-14">
  <h2
    class="text-[#7B7B7B] text-center lg:text-left uppercase mb-6 text-lg lg:text-[15px]"
  >
    {ui.selected_work.title}
  </h2>
  <div
    class="flex flex-col gap-15 lg:grid lg:grid-cols-2 lg:gap-x-3 lg:gap-y-20"
  >
    {
      ui.selected_work.projects.map((project) => (
        <ProjectCard
          name={project.name}
          description={project.description}
          url={project.url}
          videoPath={project.video_path}
          button={ui.selected_work.project_button}
        />
      ))
    }
  </div>
</section>
