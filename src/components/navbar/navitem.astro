---
interface Props {
  url: string;
  label: string;
}

const { url, label } = Astro.props;
---

<a
  href={url}
  class="text-white text-lg uppercase tracking-tighter text-center bg-transparent hover:bg-primary transition-colors w-26.5 rounded-2xl py-0.5"
  >{label}</a
>
