---
interface Props {
  name: string;
  label: string;
}

const { name, label }: Props = Astro.props;
---

<div class="flex flex-col gap-1">
  <label class="text-sm pl-3.5" for={name}>{label}</label>
  <slot />
</div>
